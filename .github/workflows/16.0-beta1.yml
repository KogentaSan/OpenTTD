name: 16.0-beta1 Windows x64 Build

on:
  workflow_dispatch:   # 只手動觸發

jobs:
  build-windows-x64:
    runs-on: windows-latest
    name: Windows x64 Build

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up MSBuild
        uses: microsoft/setup-msbuild@v1.3

      - name: Install vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git
          .\vcpkg\bootstrap-vcpkg.bat

      - name: Install dependencies via vcpkg (manifest mode)
        run: .\vcpkg\vcpkg.exe install --triplet=x64-windows-static

      - name: Configure build (CMake)
        run: cmake -A x64 -DCMAKE_BUILD_TYPE=Release -B build `
          -DCMAKE_TOOLCHAIN_FILE=${{ github.workspace }}\vcpkg\scripts\buildsystems\vcpkg.cmake `
          -DVCPKG_TARGET_TRIPLET=x64-windows-static

      - name: Build project
        run: cmake --build build --config Release

      - name: Install build
        run: cmake --install build --config Release --prefix install

      - name: Copy required DLLs (if any dynamic deps remain)
        shell: pwsh
        run: |
          $deps = @(
            "SDL2.dll",
            "liblzma.dll",
            "libpng16.dll",
            "lzo2.dll",
            "zlib1.dll"
          )

          foreach ($dll in $deps) {
            $path = "${{ github.workspace }}\vcpkg\installed\x64-windows-static\bin\$dll"
            if (Test-Path $path) {
              Copy-Item $path install/ -Force
            } else {
              Write-Output "Dependency not found (likely static linked): $dll"
            }
          }

      - name: Package zip with datetime + commit hash
        shell: pwsh
        run: |
          $timestamp = Get-Date -Format "yyyyMMddHHmmss"
          $commit = "${{ github.sha }}".Substring(0,7)
          $filename = "16.0-beta1.zip"
          Compress-Archive -Path install/* -DestinationPath $filename
          echo "ZIP_FILE=$filename" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-x64-build
          path: ${{ env.ZIP_FILE }}
